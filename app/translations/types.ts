export interface TranslationType {
  home: {
    navbar: {
      home: string,
      demos: string,
      about: string,
      contact: string
    },
    header: {
      content: string,
      projButton: string,
      cvButton: string,
    }
    profiles: {
      heading: string,
      subheading: string,
    }
    toolkit: {
      heading: string,
      subheading: string,
    }
    contact: {
      nameValue: string,
      emailValue: string,
      messageValue: string,
      submitButton: string
    }
  }

}
